# Perfil de agente: coordinador_modulo_mowa_mes

- **Proveedor:** Anthropic (sesión propia `coordinador_modulo_mowa_mes`, en segundo plano; recibe la delegación de `architec` por mensajes entre sesiones)
- **Rol/especialización actual:** coordinación del módulo `mowa_mes` (arquitectura aplicada y QA de integración)
- **Documentos que consulta habitualmente:** `docs/requerimientos-mowa-mes.md`, `docs/atomics-requirements.md` (sección 10), `docs/modules.md`, `docs/planning.md`, `docs/architecture.md`, `docs/testing.md`, `docs/contrato-api.md`, `docs/generacion-cargas.md`, `docs/mapeo-campos.md`, `docs/consulta-cartera.md`, `docs/selecciones-guardadas.md`
- **Documentos que mantiene/actualiza:** sus filas en `docs/agents/tasks.md`, su propio `profile.md`
- **Reporta a:** `architec`
- **Delega en:** `dev_backend_modulo_mowa_mes`, `dev_frontend_modulo_mowa_mes`
- **Última sesión:** 2026-09-13

## Responsabilidades

- Recibir la delegación de `architec` y mantener la coherencia del módulo con los lineamientos del proyecto: puertos/adaptadores con vertical slicing (RF-30), configuración sobre valores fijos (RF-32), contrato OpenAPI (`docs/contrato-api.md`) y reglas de datos de `/AGENTS.md`.
- Repartir el trabajo entre backend y frontend según `docs/agents/tasks.md`, respetando el orden: **el backend fija el contrato de la API antes de que el frontend lo consuma**.
- Revisar que cada entrega cumpla su criterio de listo antes de devolverla a `architec`: RF cubiertos, capas de verificación corridas y reportadas (`.claude/skills/verificar/SKILL.md`), documentación del módulo al día.
- Resolver las dudas de diseño dentro del módulo; escalar a `architec` lo que cambie requerimientos, contratos compartidos o módulos existentes.

## Reglas de trabajo

- No commitea. Entrega a `architec` un informe con los archivos tocados, las capas de verificación corridas y las que quedaron fuera.
- No inventa reglas de plataforma que no estén en `docs/requerimientos-mowa-mes.md`; si falta una, la escala.
- Datos sintéticos siempre; `archivos_anexo_chat/` se usa solo para leer estructura, nunca se copia un valor personal a código, pruebas o documentos.

## Nota de continuidad para la próxima sesión

- 2026-09-13: perfil creado. Primera delegación: módulo `mowa_mes` completo según `docs/agents/tasks.md` (T-MM-*).
- 2026-09-13: T-MM-C1 hecha → `plan.md` en esta carpeta. Backend lanzado en B1 → B2 → B3 → B6a (contrato de configuración); frontend con las reglas de `designer`, orden F4 → F3 → F2 → F5. Pendiente: respuestas de `architec` a C-1…C-6 (bloquean B4/B5); revisar la entrega de B6a y avisar a `architec`; luego lanzar B4/B5 → B6b → B7 y F3/F2.
- 2026-09-13: `architec` respondió C-1 a C-6 (volcadas en `plan.md` §4) y ajustó el plan: feriados calculados por regla, error si falta WhatsApp o supervisores. Todo se reenvió a backend y frontend. B4 y B5 quedan desbloqueadas después de B6a. F-1 (exportar `request`) y F-2 (helper de descarga) están con `designer`; si no responde antes de B6a, avisar a `architec`.
- 2026-09-13: F-1 y F-2 cerrados por `designer` en `774f39d`. B6a (B1 a B3) revisado: `verificar.ps1 -ConBase` en verde, `contrato:comprobar` OK y largo de 156 verificado. Enviado a `architec` para commit con la lista de archivos (el árbol tiene trabajo de `designer` sin commitear que no es del corte). Escalados: RF-MM-19 dice 152 y son 156; confirmar que el Speech original es inmutable. Backend lanzado a B4 → B5, que entregan juntos como B6b. Pendiente: hash del commit de B6a → avisar a frontend para F4.
- 2026-09-13: B6a commiteado por `architec` en `7849ef0` (sin push). F4 lanzado. Backend con B4 y B5, que entrega como B6b con la lista de archivos nuevos y modificados. Claves de códigos: los 8 actuales en F4 y los 4 nuevos más el tipo `error` en B6b. `architec` corrigió RF-MM-19 a 156 y dejó en RF-MM-17 que el Speech original no se modifica nunca.
- 2026-09-14: F4 entregada y revisada (54 Vitest del módulo en verde; los 8 rojos esperados son los códigos de B6b sin commitear). Enviada a `architec` con la lista de archivos y el aviso de que las claves `nav.*` de `designer` están en el mismo hunk. Luego F4 se reenvió con correcciones de `designer` (126 claves, sin `console.css`) y quedó revisada otra vez; su commit va después o junto con el de `designer` de `ui.css` (`.dialog__intro`). Tras B6b, F4 suma las 4 claves nuevas, los límites literales en la API falsa y la UI de límites por archivo. Condiciones de `architec` para F4: commit después del de `designer`, capas contra HEAD en un worktree temporal y Playwright con servidor propio (el puerto 4321 puede estar tomado por otro agente). La revisión previa de F5 agrega a la reentrega de F4 los hallazgos 3 a 6 (escala de texto, `aria-describedby` del ejemplo, rango sin repetición, `role=status`). Los hallazgos 1 y 2 (apilado responsive) van a F5 con `designer`. NVDA real queda sin correr. Pendiente: commit de F4, entrega de B6b
- 2026-09-14: B6b entregado y revisado (555 pruebas en verde y 2 rojos esperados por las claves i18n; postgres 47; migración `7a91278726ae` leída). Condiciones antes del commit: mutación manual de las reglas de B4 y B5 y las 4 claves de dev_frontend, que se agregan después del commit de F4. Escaladas a `architec`: E-1 (qué cuenta como "enviado"; propongo solo `estado = enviado`) y E-2 (carné de extranjería en `dni`; regla de plataforma). `designer` commiteó `7287f2d`; F4 se reentrega sobre un worktree desde ese commit, con los diccionarios de HEAD más el bloque `mowaMes.*`. Orden: F4 → claves → B6b. Siguiente: F3 tras B6b, F2 con el selector, B7 Playwright y F5.
- 2026-09-14: B6b cumplió la mutación manual (6 roturas, todas detectadas). E-1 y E-2 las lleva `architec` al usuario. Escenarios de B7 acordados en la nota de T-MM-B7 (C1–C4 más C1b, P1–P9, S1–S8; S8 depende de axe). F4 se reentregó verificada contra `7287f2d` (Vitest 153/153, Playwright 36/36 con servidor propio en 4399, 133 claves) y se envió a `architec` para commit. Queda fuera del corte: axe y `idioma.ts` (dependencias de `designer` sin commitear), obligatorios en F5. Decisiones del usuario: E-1 cuenta solo `estado = enviado`; E-2 carga el documento y advierte con `documento_no_estandar` (13 códigos, así que frontend agrega 5 claves); S-MM-7 y S-MM-8 confirmados. El usuario decidió que axe e `idioma.ts` entran con el commit de `/cartera`: F5 y posiblemente C2 dependen de ese commit (consultado a `architec`). F4 commiteada en `25eab2f` (sin push). `architec`: C2 se entrega con axe declarado pendiente y atado al commit de `/cartera`; F5 queda bloqueada, no hecha, y el módulo no se cierra en `planning.md` hasta reponer axe. `plan.md` va en mi corte. E-1 y E-2 aplicadas por el backend: pytest 573 en verde; `advertencias` pasa a lista; la migración `7a91278726ae` se editó en su lugar, sin commitear. Frontend entregó las 5 claves (144 y 13 por idioma) y los agregados de F4 (UI de límites y C1b). Lista unificada de B6b enviada a `architec` con los parciales marcados. B6b commiteado en `082308c` (un solo commit con tres trailers; sin push). F3 lanzado (pantalla y specs S1–S7). B7 lanzado como revisión de los specs y de la API falsa contra la API real (sin editar el frontend), suite Playwright completa y pruebas backend que falten. B7: puntos 1 a 3 hechos (revisión de la API falsa con correcciones que entran con F3, Playwright completo 54/54 y dos pruebas backend nuevas). Decidido: en el PUT de configuración, omitido conserva y null borra; entra con F3. Comprobado que la pantalla siempre manda `whatsapp_contacto`. PUT parcial aplicado por el backend con las condiciones de `architec` (pytest 583 en verde, contrato comprobado). Archivos del backend para el corte de F3: `api/mowa_mes.py`, `test_api_mowa_mes_configuracion.py`, `test_mowa_mes_campanas_postgres.py`, `openapi.json`, `contrato-api.d.ts` y `mowa-mes.md`. F3 entregada (seguimiento, 14 casos Playwright, API falsa corregida, registro de las aserciones que cambian; contra `082308c`: Vitest 186/186 y Playwright 53/53). Revisada de mi lado: 239 claves, Vitest del módulo 58/58, y `""` en WhatsApp borra igual en backend y en la API falsa. Revisión B7 de F3 hecha: nada bloqueante, Playwright 69/69, pytest 587 y postgres 50, con 4 pruebas backend nuevas. Frontend aplica 5 ajustes menores de la API falsa (codigo, problemas de segmentos, fechas UTC, mes en Lima, límite por defecto) y reenvía el diff. Ajustes aplicados (Playwright 53/53 contra `082308c`; fechas UTC que cruzan la medianoche fijadas en S1 y S3 con `timezoneId` Lima). Lista unificada del corte F3 enviada a `architec`. Pendiente: commit de F3, selector de `designer` → F2 → cierre de B7 → C2. Antes enviado: lista unificada del corte F3 (frontend más los archivos del backend del PUT parcial) a `architec`, selector de `designer` para F2, cierre de B7 con P1–P9 y después C2 (con axe pendiente atado a `/cartera`) → commit de B6b (con E-1 y E-2 resueltas) → F3 y F2. (B4 y B5) y después F3 → F2 (espera el selector) → F5 y B7.
