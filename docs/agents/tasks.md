# Tablero de subtareas multiagente

> Escritura aditiva: cada agente agrega o actualiza sus propias filas; no
> reescribe las de otros (skill `coordinador-multiagente`, sección 6).

Estados válidos: `pendiente`, `en progreso`, `hecho`, `bloqueado`.

## Módulo `mowa_mes` — primer conector SMS (2026-09-13)

Cadena de delegación: `architec` → `coordinador_modulo_mowa_mes` → `dev_backend_modulo_mowa_mes` y `dev_frontend_modulo_mowa_mes` (con `designer` para lo visual). Requerimientos: `docs/requerimientos-mowa-mes.md` y RF-37 a RF-41 en `docs/atomics-requirements.md`. Diseño previsto: `docs/modules.md`, sección 5.1.

**Orden obligatorio:** el backend fija el contrato de la API (T-MM-B6) antes de que el frontend lo consuma (T-MM-F2 en adelante). Ningún agente delegado commitea: entrega a su coordinador, y `architec` verifica, commitea con el trailer `Agente:` de quien escribió e informa al usuario.

| ID | Descripción | Rol requerido | Agente asignado | Documentos relacionados | Estado | Notas de continuidad |
|---|---|---|---|---|---|---|
| T-MM-00 | Requerimientos generales de gestiones digitales (RF-37 a RF-41) y específicos de `mowa_mes` (RF-MM-01 a RF-MM-22), con decisiones y evidencia de los ejemplos | requisitos | architec | `atomics-requirements.md`, `requirements.md`, `requerimientos-mowa-mes.md` | hecho | Decisiones del usuario del 2026-09-13 en la sección 6 del documento del módulo; supuestos S-MM-1 a S-MM-6 por confirmar |
| T-MM-01 | Registro del módulo y diseño previsto en `modules.md`; avance en `planning.md`; perfiles y tablero en `docs/agents/` | arquitectura / docs | architec | `modules.md`, `planning.md`, `agents/` | hecho | |
| T-MM-C1 | Plan de ejecución del módulo: confirmar el diseño de `modules.md` §5.1, fijar la secuencia backend → contrato → frontend y lanzar a los agentes | arquitectura | coordinador_modulo_mowa_mes | `requerimientos-mowa-mes.md`, `modules.md`, `architecture.md` | hecho | Plan en `docs/agents/coordinador_modulo_mowa_mes/plan.md`: diseño §5.1 confirmado con precisiones; contrato en dos cortes (B6a configuración → F4; B6b campaña/reporte → F3, F2); consultas C-1 a C-6 escaladas a `architec` |
| T-MM-B1 | Calendario laboral: siguiente día gestionable (lunes a viernes sin feriados nacionales de Perú), feriados configurables y persistidos (RF-MM-08) | arquitectura / backend | dev_backend_modulo_mowa_mes | `requerimientos-mowa-mes.md` §2 | hecho | 2026-09-13, entregado al coordinador (sin commit). Feriados de ley calculados por regla (DL 713 art. 6 con Leyes 31530, 31788, 31822; Pascua por Meeus) y tabla `calendario_excepcion` solo con excepciones `agregado`/`retirado`; siguiente día gestionable estrictamente posterior, hoy en America/Lima. API: `/calendario/feriados`, `/calendario/excepciones`, `/calendario/siguiente-dia-gestionable`. Detalle en `docs/mowa-mes.md` §2 |
| T-MM-B2 | Supervisión digital transversal: supervisores por defecto configurables, ajuste por campaña, DNIs secuenciales, campos copiados de la primera fila válida, inclusión en el primer archivo y en el resumen (RF-37 a RF-41) | backend | dev_backend_modulo_mowa_mes | `atomics-requirements.md` §10 | hecho (núcleo y configuración) | `app/core/services/gestiones_digitales/`; sin números reales en código ni pruebas. 2026-09-13, entregado: validación RF-02, DNIs secuenciales por orden de procedencia, `filas_supervision` con plantilla, `SinSupervisores`/`SinProductosCargables`; `GET`/`PUT /supervisores` con `documento`. Inclusión en el primer archivo y en el resumen queda para B4 |
| T-MM-B3 | Speech: días de atraso ajustados y segmento, estructura por partes sobre el motor de mapeo, versiones trazables con `Speech original` como defecto, variable de WhatsApp, límites de 150/160 (RF-MM-14 a RF-MM-19) | backend | dev_backend_modulo_mowa_mes | `requerimientos-mowa-mes.md` §4, `mapeo-campos.md` | hecho | 2026-09-13, entregado. Segmentos como datos, S-MM-5/S-MM-6, mensaje con `GeneradorCargas` sin tocar el motor, `[whatsapp]` sin enlace roto (`FaltaWhatsapp`), 150/160, versiones con `usada_en` (la marca B4 con `marcar_speech_usado`) y Speech original también inmutable. Con el Speech original, `9 a 30` mide 156, no ~152 como dice RF-MM-19 |
| T-MM-B4 | Campaña: inputs registrados, armado de la carga desde la selección, exclusiones con motivo, supervisión al inicio del primer archivo, división por 50 000 registros y 2 MB, archivo `.xlsx` en formato MES, límite mensual, persistencia y endpoints de previsualización, creación, listado y descarga (RF-MM-01 a RF-MM-13) | backend | dev_backend_modulo_mowa_mes | `requerimientos-mowa-mes.md` §1–3, `generacion-cargas.md`, `consulta-cartera.md`, `selecciones-guardadas.md` | hecho | 2026-09-14, entregado al coordinador como corte B6b (sin commit). Recorrido paginado extraído a `seleccion_cartera/recorrido.py` (C-2); exclusiones con un solo motivo; supervisión al inicio con la primera fila cargada; WhatsApp faltante y sin supervisores como errores; límite por mes de envío con 409 sin confirmar; huella del speech con 409 si cambió; división por filas y bytes reales con límites en la configuración; archivos en PostgreSQL; descarga con `X-Mowa-Mes-*`. Medición con 46 000 filas en `docs/mowa-mes.md` §13 |
| T-MM-B5 | Reporte de enviados: lectura y validación, asociación a la campaña por `id` de MES, conciliación con mensaje normalizado, cifras de cargados y enviados (RF-MM-02, RF-MM-20 a RF-MM-22) | backend | dev_backend_modulo_mowa_mes | `requerimientos-mowa-mes.md` §5 y §7 | hecho | 2026-09-14, entregado con B4 como corte B6b. Lector calamine con las 8 columnas; cada `id` de MES asociado a la campaña, 409 salvo `reemplazar=true`; conciliación multiconjunto con normalización en los dos lados; advertencia `id_sin_correspondencia`. 2026-09-14, decisiones del usuario aplicadas: E-1 (enviado = emparejada con estado `enviado`; otro estado cuenta como no enviada; cifras por `id` siguen contando las emparejadas) y E-2 (documento no DNI/RUC se carga con advertencia `documento_no_estandar`, catálogo de 13 códigos; `mowa_mes_fila_cargada.advertencias` pasa a lista). Mutaciones E-1, E-2a y E-2b detectadas |
| T-MM-B6 | Contrato: `contratos/openapi.json` y tipos del frontend regenerados y comprobados; documento técnico `docs/mowa-mes.md` | backend / docs | dev_backend_modulo_mowa_mes | `contrato-api.md` | hecho (B6a en `7849ef0`; B6b entregado sin commit) | 2026-09-14: B6b entregado (openapi.json y `contrato-api.d.ts` regenerados, `docs/mowa-mes.md` §9 a §14). La prueba del catálogo contra i18n espera las claves `mowaMes.codigo.*` de dev_frontend. 2026-09-13: B6a entregado (openapi.json y `contrato-api.d.ts` regenerados, `docs/mowa-mes.md` con el primer corte). Para B6b: cabeceras de descarga con prefijo único `X-Mowa-Mes-`. Dos cortes: B6a (configuración, feriados, supervisores, speech) desbloquea F4; B6b (campaña, archivos, reporte, conciliación) desbloquea F3 y F2 |
| T-MM-B7 | Calidad de extremo a extremo: cadena completa por HTTP contra PostgreSQL (selección → campaña → archivos → reporte → conciliación) y escenarios Playwright del flujo del módulo acordados con frontend; `verificar.ps1 -ConBase` en verde | qa | dev_backend_modulo_mowa_mes | `testing.md`, skill `verificar` | en progreso | 2026-09-14: la cadena por HTTP contra PostgreSQL está hecha (`tests/test_mowa_mes_campanas_postgres.py`). Mutación manual de B4/B5 (orden de exclusiones, multiconjunto, normalización de cada lado, huella, posición de la supervisión, mes de imputación): las 6 detectadas. Escenarios Playwright **acordados por escrito con dev_frontend el 2026-09-14**, sobre `e2e/api-falsa-mowa-mes.ts`. Criterios: tipos de `contrato-api.d.ts`; datos sintéticos (900000xxx, titulares "ZZPRUEBA…", id de MES 99xxxxxxx); se verifica la frase en español, como en los demás specs, y la cobertura por clave la dan `test_codigos_mowa_mes.py`, el espejo `i18n-codigos-mowa-mes.test.ts` y un escenario en inglés por pantalla. **F4** (`mowa-mes-configuracion.spec.ts`): C1 límite y WhatsApp; C1b límites por archivo (con B6b; rechazo en cliente por encima de 50 000 / 2 000 000); C2 supervisores (validación de forma en cliente y 400 de RF-02 con detalle); C3 speech (original y usadas se guardan con POST y `basada_en_id`; el 409 de una versión usada mientras se editaba conserva lo escrito); C4 feriados y excepciones. **F2**: P1 previsualización (supervisión primero, verificada por orden de filas; archivos previstos rotulados como estimación); P2 errores de campaña con Crear deshabilitado; P3 límite con `openWhenFree`, preseleccionado no confirmar, 409 y luego 201; P4 huella: 409 y Crear deshabilitado hasta volver a previsualizar; P5 opciones deshabilitadas; P6 siguiente día gestionable y "enviar ahora" sin fechas; P7 descarga (nombre desde `Content-Disposition`, resumen con el prefijo `X-Mowa-Mes-` vía `descargarArchivo`); P8 sin versión vigente, 404; P9 selección desde el selector de designer (`seleccioncambiada`, modo compacto), y una selección que no aplica no deja previsualizar. **F3**: S1 listado paginado; S2 límite por mes; S3 importar y conciliar; S4 reimportar con `reemplazar=true`; S5 `id_sin_correspondencia`; S6 400 con detalle y 413 con mensaje propio traducido; S7 exclusiones filtradas y paginadas. S8 (axe) queda fuera del corte hasta que designer commitee `accesibilidad.spec.ts` con `@axe-core/playwright`. Abierto: lo que dependa de las consultas 5 (qué es enviado) y 6 (carné de extranjería). F2 y F3 empiezan con el commit de B6b y el cierre de 5 y 6. 2026-09-14, tras `082308c`: punto 1, revisión de `api-falsa-mowa-mes.ts` y del spec de F4 contra el backend, diferencias enviadas a dev_frontend (ejemplo "XXXXXXXX"/"dd/mm/yyyy", `largo_maximo` sin WhatsApp, PUT sin `whatsapp_contacto` borra, validaciones 400/404 y lo garantizado para F3 y F2); punto 3, pruebas backend nuevas `test_un_put_sin_whatsapp_lo_borra_y_sin_limites_los_conserva` y `test_cadena_completa_con_estados_distintos_de_enviado` (entran en el corte de F3); punto 2, suite Playwright completa 54/54 con build fresco y servidor propio en el puerto 4391 (configuración temporal fuera del repo). Revisión de F3 (S1–S7, 14 casos): sin diferencias bloqueantes; 5 supuestos de dev_frontend confirmados; ajustes menores de la API falsa enviados (filtro `codigo` con los 13 códigos, paginación por defecto 50/100, fechas en UTC, mes por defecto en Lima, problemas de segmentos faltantes); suite Playwright 69/69 con servidor propio; pruebas backend nuevas: WhatsApp `""` y `"   "` borran (con mutación), exclusiones con código fuera del catálogo o no de exclusión, conciliación sin reporte, límite de un mes sin campañas (PostgreSQL). Pendiente: P1–P9 (F2) |
| T-MM-F1 | Coordinación visual con `designer`: ubicación del módulo en la navegación, patrones y componentes a reutilizar, cambios en archivos compartidos | ui | dev_frontend_modulo_mowa_mes | `design_ui/brand_guide.json`, `/DESIGN.md` | hecho | Reglas de `designer` (vía `architec`, 2026-09-13), vinculantes: rutas `/mowa-mes/campana`, `/mowa-mes/seguimiento`, `/mowa-mes/configuracion`; `designer` agrega "Canales > MOWA MES" en `Shell.astro` (frontend no lo toca); API del módulo en `src/lib/api-mowa-mes.ts` (nada en `api.ts`); `contrato-api.d.ts` solo con `npm run contrato`; i18n solo agregando bajo `mowaMes.*`; estilos en `src/styles/mowa-mes.css` (no tocar `ui.css`/`tokens.css`); radio 0 estructural, píldora solo en botones/badges/tags/puntos; montos con `format.ts`; diálogos con `openWhenFree`; previsualización como tabla densa de incidencias de `/cargas`; descargas con el patrón de `/generacion`; Playwright con `e2e/api-falsa-mowa-mes.ts`; teléfonos sintéticos `900000xxx` |
| T-MM-F2 | Pantalla de campaña MOWA MES: base de la campaña (fecha de corte y selección), inputs de la plataforma, supervisores y speech, previsualización con exclusiones y advertencias, generación y descarga de archivos | ui | dev_frontend_modulo_mowa_mes | `requerimientos-mowa-mes.md` §1–4 | en progreso | 2026-09-16, parte A entregada al coordinador (sin commit, verificada en worktree desde `f86308b`): `previsualizarCampana`/`crearCampana` en `api-mowa-mes.ts`; lógica pura en `src/lib/mowa-mes-campana.ts` (borrador→petición, `problemasCampana` con las mismas reglas que `_validar_opciones` y `_fecha_envio`, hora de Lima explícita sin depender del navegador, huella, lectura de la previsualización); bloque `mowaMes.campana.*` (4 claves); rutas nuevas en la API falsa, en revisión de dev_backend (B7) antes de entregar. Sin página ni specs Playwright: van en la parte B con el selector de designer. Tercera del frontend. Espera T-MM-B6b **y** el commit de `designer` del selector de selección (`src/lib/seleccion.ts`, `src/scripts/selector-seleccion.ts`, evento `seleccioncambiada`), que se monta en modo compacto y no se duplica. F-1 y F-2 ya no bloquean (commit `774f39d` de `designer`): `request` exportado y descarga compartida en `src/lib/descargas.ts` |
| T-MM-F3 | Seguimiento: listado de campañas, cargados y enviados, importación del reporte de enviados y conciliación, consumo del límite mensual | ui | dev_frontend_modulo_mowa_mes | `requerimientos-mowa-mes.md` §1 y §5 | en progreso | 2026-09-14: arrancada con B6b en `082308c`. `/mowa-mes/seguimiento` (límite por mes, campañas paginadas con productos/supervisión/total, archivos con `descargarArchivo` y prefijo `X-Mowa-Mes-`, importación multipart con 409 y reemplazar, 413 propio, conciliación con enviados = estado "enviado" y "coinciden por id" rotulado aparte, exclusiones filtrables); S1–S7 en `e2e/mowa-mes-seguimiento.spec.ts`; en el mismo corte, correcciones de la API falsa pedidas por B7 (ejemplo XXXXXXXX y dd/mm/yyyy, largo con enlace de 26, PUT parcial de configuración, Speech N, validaciones 400/404/409/422). Entrada en la navegación pedida a designer. Entregado al coordinador (sin commit), verificado en worktree desde `082308c`: `npm run verificar` completo, Vitest 186/186, Playwright 53/53 con servidor propio. Revisión B7 de dev_backend sin bloqueantes; ajustes menores de la API falsa aplicados (codigo con los 13 del catálogo, un solo problema de segmentos, fechas UTC cruzando medianoche con `timezoneId` America/Lima, mes actual por defecto, límites 50/100). Pendiente: `.mm-pager` → `.pager` y el enlace de navegación cuando designer commitee /cartera. Segunda del frontend. Espera T-MM-B6b; no depende del selector. F-1 y F-2 ya no bloquean (commit `774f39d` de `designer`): usa `request` y `src/lib/descargas.ts` |
| T-MM-F4 | Configuración: supervisores por defecto, versiones de speech con previsualización del largo por segmento, número de WhatsApp de contacto, feriados | ui | dev_frontend_modulo_mowa_mes | `requerimientos-mowa-mes.md` §2 y §4 | hecho (entregado al coordinador, sin commit) | 2026-09-14: `/mowa-mes/configuracion` con plataforma, supervisores, speech con previsualización de largo y calendario; `src/lib/api-mowa-mes.ts`, `src/lib/mowa-mes.ts`, `src/styles/mowa-mes.css`, bloque `mowaMes.*` al final de `es.json`/`en.json` (8 códigos de B6a), Vitest (`api-mowa-mes`, `mowa-mes`, espejo `i18n-codigos-mowa-mes`), Playwright con `e2e/api-falsa-mowa-mes.ts` y `e2e/mowa-mes-configuracion.spec.ts` (incluye axe). Rojo esperado en el árbol: 8 casos del espejo por los 4 códigos de B6b sin commitear. F4 commiteada en `25eab2f`. 2026-09-14, entregado al coordinador (sin commit, contra el árbol con B6b): las 5 claves de códigos y los límites por archivo en Plataforma con C1b y la API falsa con campos literales; Vitest 209/209, pytest `test_codigos_mowa_mes` 4 passed, Playwright del módulo 19/19. Detalle de lo que se acordó: 5 claves de códigos (`id_sin_correspondencia`, `falta_whatsapp`, `sin_supervisores`, `sin_productos_cargables` y, por decisión E-2 del usuario, `documento_no_estandar` como advertencia; esta última solo cuando el backend la agregue al enum), más el tipo error; para F3 (E-1): enviado = fila emparejada con estado "enviado", cualquier otro estado es cargado no enviado y va en `por_estado`; escribir literales `registros_por_archivo`/`bytes_por_archivo` en la respuesta falsa (hoy spread puente, condición de designer); agregar a Configuración esos dos límites, editables solo hacia abajo (≤ 50 000 filas, ≤ 2 000 000 bytes). Depende del commit de designer de `ui.css` (`.dialog__intro`) y de su enlace en `Shell.astro`. Primera del frontend. Espera T-MM-B6a; no depende del selector. 2026-09-13 (dev_frontend): estudio previo hecho (plan, `cargas.astro`, `dialogs.ts`, `format.ts`, `api.ts`, `e2e/api-falsa.ts`, Vitest `tests/nucleo` y `tests/dom`); sin código escrito hasta B6a. Consultas al coordinador: (a) `request` no se exporta desde `api.ts`, así que `api-mowa-mes.ts` no puede importarlo sin tocar ese archivo; (b) no existe todavía la pantalla `/generacion` ni `/cartera` en `frontend/`, así que falta el patrón de descarga de referencia. Resueltas por `architec`: F-1, exportar `request` se pide directamente a `designer` (pedido enviado 2026-09-13) y frontend no toca `api.ts`; F-2, la descarga se implementa contra el contrato real. Cerradas por `designer` en el commit `774f39d` (sin push): `api.ts` exporta `request` y `requestRespuesta`; la descarga compartida vive en `src/lib/descargas.ts` (`descargarArchivo(path, init, prefijoResumen)`, `guardarArchivo`) y `api-mowa-mes.ts` solo arma el path y el body y la llama con `'X-<Prefijo>-'`, sin duplicarla. Reglas nuevas a mostrar: sin WhatsApp y con speech que lo usa, error en la previsualización y 400 al crear; sin supervisores configurados, la campaña no se crea; código de exclusión nuevo `falta_documento` (i18n `mowaMes.*`). Claves de códigos (acordado con coordinador y backend): formato plano `mowaMes.codigo.<codigo>`; F4 entrega exactamente los 8 de la enum `CodigoMowaMes` commiteada con B6a, y los 4 nuevos (`id_sin_correspondencia`, `falta_whatsapp`, `sin_supervisores`, `sin_productos_cargables`) van en el corte B6b; prueba espejo en `tests/nucleo` que lee la enum del backend (faltantes y sobrantes). Todas las claves `mowaMes.*` en un bloque contiguo al final de `es.json` y `en.json`, sin reformatear lo existente |
| T-MM-F5 | Certificación de calidad del frontend: pruebas unitarias Vitest (núcleo y DOM), i18n es/en completo, `astro check` sin errores, accesibilidad y anchos del proyecto; `npm run verificar` en verde | qa / ui | dev_frontend_modulo_mowa_mes | `testing.md`, skill `verificar` | bloqueado | Bloqueada por el commit de /cartera de designer (trae `@axe-core/playwright`, `e2e/accesibilidad.spec.ts` y `tests/nucleo/idioma.ts`; decisión del usuario de no adelantarlos). Por decisión de architec, F2, F3, B7 y C2 no esperan ese commit. 2026-09-14, revisión previa sobre F4 (script aparte, sin tocar el corte): anchos 360/768/1280 sin scroll horizontal de página; Tab lógico con nombre y foco visible; diálogo con foco dentro y retorno. Hallazgos a corregir: (1) tabla de speech con Largo/Estado fuera de vista bajo 64rem, proponer apilado a designer; (2) supervisores a 360 con select truncado y Documento fuera de vista; (3) meta "Creada…", "Largo calculado…" y "Siguiente día…" fuera de la escala tipográfica; (4) fila "Ejemplo" sin relación accesible con su segmento (aria-describedby); (5) nombre de fila duplicado "9 a 30 9 a 30 días"; (6) cambios de nivel por fila no anunciados (resumen en role=status). Falta: lector real (NVDA) y zoom 200% (aproximable con viewport de 640 px). Hallazgos 3 a 6 ya resueltos en la reentrega de F4; 1 y 2 con el patrón `.table--apilable` / `.table--apilable-ancha` de designer cuando esté commiteado. Obligatorios de F5 por el corte F4 contra HEAD: reponer los 3 casos axe de `/mowa-mes/configuracion` en `e2e/accesibilidad.spec.ts` cuando designer commitee `@axe-core/playwright`; reemplazar la copia provisional de `tests/nucleo/idioma.ts` en `tests/nucleo/mowa-mes.test.ts` por el import. Al cerrar F2; Vitest para `src/lib`, Playwright con `e2e/api-falsa-mowa-mes.ts` (escenarios del flujo acordados con backend, T-MM-B7) |
| T-MM-C2 | Revisión integral del módulo contra los RF y entrega a `architec` con el informe de archivos y capas de verificación | qa | coordinador_modulo_mowa_mes | todos los anteriores | pendiente | |
