# CLAUDE.md

@AGENTS.md

Instrucciones específicas para Claude Code (además de lo anterior):

- Al trabajar dentro de `frontend/`, respeta también `frontend/AGENTS.md`.
- La documentación completa de arquitectura, requerimientos, módulos y planificación vive en `docs/`. Léela antes de proponer cambios estructurales.
- Este workspace corre en Windows (PowerShell). Usa las herramientas dedicadas (Read/Edit/Grep/Glob) en vez de comandos de shell equivalentes.
