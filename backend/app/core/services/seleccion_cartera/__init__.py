"""Caso de uso: seleccion, filtrado, segmentacion y metricas de cartera.

Vertical slice de la Fase 2 (RF-04 a RF-06, RF-08, RF-25 a RF-28):

- campos.py    -- que campos existen, que operadores admite cada uno y como se
                  convierten los valores que llegan como texto.
- servicio.py  -- valida la consulta y la delega al repositorio.

La cartera de una fecha sale siempre de su version vigente. El control de
gestiones multicanal (RF-07) queda fuera hasta que existan las gestiones.
"""
